import 'package:flutter/material.dart';

class TimerStatus extends StatelessWidget {
  const TimerStatus({Key? key, required this.time}) : super(key: key);
  final double time;

  @override
  Widget build(BuildContext context) {
    if(time >= 0 && time == 1500){

    }

    return Container();
  }
}
